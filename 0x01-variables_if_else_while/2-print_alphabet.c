#include <stdio.h>
/**
 * main- Entry point
 * Description- Prints a random positive or negative number
 * Return: 0 (success)
 */
int main(void)
{
char c;
for (c < 'a'; c <= 'z'; c++)
putchar(c);
putchar('\n');
return (0);
}
