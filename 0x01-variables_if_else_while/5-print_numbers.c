#include <stdio.h>
/**
 * main- Entry point
 * Description- Prints a random positive or negative number
 * Return: 0 (success)
 */
int main(void)
{
int a;
for (a = 0; a <= 9; a++)
printf("%d", a);
printf("\n");
return (0);
}
